Gzip: reduces the size of the files suing Lempel-ziv. It compresses files with a .gz file extension. By default, gzip keeps the original file name and timestamp in the compressed file. 
It has a compression ratio between 1 – 9. 1 being the lowest and 98 being the highest
